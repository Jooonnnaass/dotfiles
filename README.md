#Dotfiles of Jooonnnaass

Install via `install.sh`. The script installs everything needed if possible and adds the configuration files via symlinks.

##Mac OS

Dependencies:

- Homebrew

What will be installed ?

- Wezterm
- Neovim
- wezterm.lua
- nvim config file
